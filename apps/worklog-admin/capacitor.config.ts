import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "io.ionic.starter",
  appName: "worklog-admin",
  webDir: "../../dist/apps/worklog-admin",
  bundledWebRuntime: false,
};

export default config;
