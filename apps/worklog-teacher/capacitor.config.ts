import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "io.ionic.starter",
  appName: "worklog-teacher",
  webDir: "../../dist/apps/worklog-teacher",
  bundledWebRuntime: false,
};

export default config;
